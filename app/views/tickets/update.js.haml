var $status = $("body").find("##{dom_id(@ticket)}").find(".btn-status");$(function(){if ( $status.hasClass("btn-danger") ){$status.attr("class","btn btn-status btn-success").val("Mark Resolved");}else{$status.attr("class","btn btn-status btn-danger").val("Mark Un-Resolved");}});